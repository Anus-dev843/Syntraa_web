# Copy this file to ".env.local" and fill in real values (never commit .env.local).

# MongoDB Atlas — one line. App code uses database name "syntraa" via the native driver (src/lib/db.ts).
# Path in URI may be anything; `getSyntraaDb()` always selects `syntraa`.
MONGODB_URI=mongodb+srv://USER:PASSWORD@cluster0.xxxxx.mongodb.net/?retryWrites=true&w=majority

# Cloudinary (https://console.cloudinary.com → API Keys). Cloud name has no "@" prefix.
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=
